# Arr Stack Connector beta archive

Unindexed historical metadata for development build `0.2.0-beta.1`. Dispatcharr does not advertise this directory; the active development entry now reuses the released stable manifest from `main`.
